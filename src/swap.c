#include "../inc/push_swap.h"

/* t_stack	*ft_swap(t_stack *a)
{
	int aux;

	if (ft_stacksize(a) < 2 || !a)
		return (NULL);
	aux = a->content;
	a->content = a->next->content;
	a = a->next;
	a->content = aux;
	return (a);
} */